export interface Country {
    id?: string;
    created_at?: Date;
    updated_at?: Date;
    name: string
    code: string
};

export interface Language {
    id?: string;
    created_at?: Date;
    updated_at?: Date;
    name: string;
    code: string;
};